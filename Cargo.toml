[workspace]
members = [
    "src/libkrun",
    "src/krun_input",
    "src/krun",
    "examples/rust_vm",
]
default-members = ["src/libkrun", "src/krun_input", "src/krun", "examples/rust_vm"]
exclude = ["examples/gtk_display"]
resolver = "2"

[workspace.dependencies]
proptest = { version = "=1.11.0", default-features = false, features = ["std"] }

[patch.crates-io]
# Native private file views are required by the Windows backing implementation.
msb-vm-memory = { git = "https://github.com/superradcompany/rust-vmm", rev = "f798d4f274db22a3c458ba756900db2cd03e6fe9" }

# Keep every vm-memory consumer, including msb-imago, on msb-vm-memory. Cargo treats the upstream
# and compatibility packages as different type identities even though they expose the same API.
[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true
